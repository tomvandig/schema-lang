import colors from "./shared/colors";
import { f32, i32 } from "./shared/primitives";

export const geometry = {
    "ifc::geometry": [
        {
            vertices: [f32],
            indices: [i32],
            color: colors
        }
    ]
}